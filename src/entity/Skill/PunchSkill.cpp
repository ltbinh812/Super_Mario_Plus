#include "PunchSkill.h"
#include "Player.h"

void PunchSkill::execute(Player& player) {
    // TODO: Spawn a fireball projectile in the facing direction.
    // For now, the skill state handles animation and duration.
    // Future: create a Fireball entity and add it to the world.
}
